# Development/Cloud Environment Configuration
# Use with: docker-compose --env-file .env.dev up

# API Server URL
# Empty string = use relative URLs (nginx proxies to server)
# For standalone client: AGENT_SERVER_URL=http://your-api-server:3000
AGENT_SERVER_URL=
