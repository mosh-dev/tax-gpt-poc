- First Lets Migrate the client application to react from angular, then
- I have added a tax-gpt-ux folder with ui and ux design for the agent lets follow the design and integrate those features one by one.
- organize the server codebase using clean architecture.
- for mongo-repository use some generic method for crud  and use the mongo repository with file repository and chat repository.
- for the mongo-repository if the connection is not active i dont need fallback just reconnect and then perform the action.
- use linting for proper code type safety right now there are random type mismatch error popping up in my ide for many ts files.

Implement all these step by step.

Next steps -

- Create another page to upload pdf txt and doc file to train the model using vector-database and rag.(Use mongodb as DB)
- And configure mastra to utilize that vector-db as memory.(see - https://mastra.ai/docs/memory/overview, https://github.com/mastra-ai/mastra/blob/main/docs/src/content/en/docs/memory/storage/memory-with-mongodb.mdx)

-- Workflow design

- Current setup does not handle deployment environment properly,
lets configure the client and server with proper setup for everything each endpoint  mongodb connection openai endpoint apikey(I might use openai gpt model from my subscription)
and other things which might be relevant.
- Add Some basic guardrails for the agent to prevent prompt injection since now we are embedding document during chat.


