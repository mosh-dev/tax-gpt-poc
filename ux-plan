Right now there is no database setup to tract conversation history.
- I have setup a mongodb instance running on localhost:27017 port. Lets use this to keep track of chat history so that i can resume from where i started.
- Lets handle file upload properly with db tables. So that when the system will be deployed to docker it giver proper url for the file.
- All generated pdf will be uploaded to storage as file. Lets use one directory as for files and one table to keep track of them.
- Also i have added a tax-gpt-ux folder with ui and ux design for the agent lets follow the design and integrate those features one by one.